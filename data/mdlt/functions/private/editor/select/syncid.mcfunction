#> mdlt:private/editor/select/syncid
# to execute from the model
# sync id of the player (@a[tag=temp_player]) and the id of the selected model (@s)

scoreboard players operation @a[tag=temp_player] mdlt.model.id = @s mdlt.model.id